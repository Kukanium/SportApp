Для начала работы с приложением и проверки его работоспособности необходимо в коде каждого из компонентов внести API token.
Для этого в каждом компоненте в переменную ApiKey внесите собственный токен.
